<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="br.com.android.posologia">
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <receiver android:name=".util.BroadcastReceiverAlarme">
            <intent-filter>
                <action android:name="ALARME_DISPARADO"/>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </receiver>

        <activity
            android:name=".view.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/MyTheme"/>

        <activity
            android:name=".view.PosologiaNewActivity"
            android:label="@string/app_title_posologia_new" />

        <activity
            android:name=".view.LoginActivity"
            android:label="@string/app_title_pessoa"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".view.CadastroActivity"
            android:label="@string/app_title_pessoa_new"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".view.MedicamentoNewActivity"
            android:label="@string/app_title_medicamento_new"/>
    </application>
</manifest>
